import 'package:flutter/material.dart';
import '../theme.dart';

class Flip7Logo extends StatelessWidget {
  const Flip7Logo({super.key});

  @override
  Widget build(BuildContext context) {
    // Placeholder using generated PNG asset. Replace with actual SVG implementation later.
    return Image.asset('assets/flip7/flip7_logo_placeholder.png', width: 120, height: 120);
  }
}
